<% if flash[:notice] %>
      <div class="flash">
        <%= flash[:notice] %>
      </div>
    <% end %>
<h2>Title</h2>
<p><%= @book.title %></p>
<h2>Body</h2>
<p><%= @book.body %></p>
<p><%=link_to "edit", edit_book_path(@book.id), class: "edit#{@book.id}" %>
   |
   <%=link_to "back", books_path(@book.id), class: "back" %>
</p>